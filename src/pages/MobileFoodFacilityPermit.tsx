import { Box } from "@mui/material";

export const MobileFoodFacilityPermit = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexFlow: 1,
        minHeight: "100vh",
        flexDirection: "column",
        height: "100%",
        width: "100vw",
      }}
    />
  );
};
